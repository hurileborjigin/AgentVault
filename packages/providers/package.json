{
  "name": "@agents-vault/providers",
  "version": "1.0.0",
  "description": "OpenAI and Azure OpenAI embedding and answer providers for Agent Vault",
  "license": "MIT",
  "type": "commonjs",
  "repository": {
    "type": "git",
    "url": "https://github.com/agents-vault/agents-vault.git",
    "directory": "packages/providers"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agents-vault/core": "workspace:*",
    "@agents-vault/shared": "workspace:*",
    "openai": "^6.7.0"
  }
}
